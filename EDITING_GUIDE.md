# 🎨 空间编辑功能使用指南

## 快速开始

### 打开编辑器
1. 进入应用后会看到空间选择界面
2. 找到要编辑的空间
3. 点击空间卡片右侧的**编辑按钮 (⚙️)**

### 编辑器界面布局
```
┌─────────────────────────────────┐
│  编辑空间                    ✕  │
├─────────────────────────────────┤
│  🎵 背景音乐 (BGM)               │
│  💾 当前：bgm_xxxx.mp3           │
│  [上传] [清除]                   │
│                                   │
│  🌍 环境音效 (按方向)            │
│  ┌─ NORTH (北) ─────────────┐   │
│  │ 当前：ambience_north.mp3 │   │
│  │ [上传] [删除]              │   │
│  │ [文本描述输入框]            │   │
│  └────────────────────────────┘   │
│  ... (东西南)                     │
│                                   │
│  💬 引导点 / 🔊 语音点            │
│  [+ 添加]                         │
│                                   │
├─────────────────────────────────┤
│          [💾 保存] [取消]          │
└─────────────────────────────────┘
```

## 分章节功能说明

### 📻 背景音乐编辑

**目的**: 设置空间进入时的背景音乐

**操作步骤**:
1. 点击 **"上传背景音乐"** 按钮
2. 选择音频文件 (MP3, WAV 等)
3. 选中后文件名会显示在上方
4. 点击 **"清除"** 可移除当前背景音乐

**提示**:
- 背景音乐会在进入空间时自动播放
- 支持循环播放

---

### 🌍 环境音效编辑 (四个方向)

**目的**: 为空间的四个方向设置不同的环境音，根据用户观看方向自动切换

**四个方向含义**:
- **NORTH (北)** - 上方 (向上看时)
- **SOUTH (南)** - 下方 (向下看时)
- **EAST (东)** - 右方 (向右看时)
- **WEST (西)** - 左方 (向左看时)

#### 为某个方向添加环境音:

1. 找到对应方向的卡片 (例如 NORTH)
2. 点击 **"上传"** 按钮
3. 选择音频文件
4. 在文本框中输入该方向的**描述** (例如"雨声" "风声")
5. 点击保存

#### 修改环境音描述:

1. 直接在文本框中修改内容
2. 这个文本会显示在语音点/引导点的关联选项中

#### 删除某个方向的环境音:

1. 点击 **"删除"** 按钮
2. 该方向将没有环境音

---

### 💬 引导点编辑

**目的**: 添加/编辑空间内的引导问题或提示，用户点击时显示文本气泡

#### 查看现有引导点:
- 显示列表中所有已有的引导点
- 每个引导点显示其**位置 [x, y, z]** 和**文本内容**

#### 添加新引导点:

1. 点击 **"+ 添加引导点"** 按钮
2. 新项目会附加到列表底部，位置默认为 `[0, 1.2, -2]`
3. 编辑**文本内容** - 输入用户点击时要显示的文字
4. 选择**关联环境音** (可选) - 选择点击时要播放的环境音
5. 点击保存

#### 编辑现有引导点:

1. 在要编辑的引导点卡片中修改文本
2. 从下拉菜单重新选择关联的环境音
3. 点击保存

#### 删除引导点:

1. 在要删除的引导点上点击 **"删除"** 按钮
2. 点击保存

**提示**:
- 引导点文本建议简洁，便于气泡显示
- 可选择与之相关的环境音来增强沉浸感

---

### 🔊 语音点编辑

**目的**: 添加/编辑空间内显示为喇叭图标的交互点，点击播放相关音效

#### 操作方法类似引导点:

1. 点击 **"+ 添加语音点"** 按钮
2. 编辑**文本内容**
3. 选择**关联环境音**
4. 点击保存

**区别**:
- 语音点在3D空间中显示为 🔊 喇叭图标
- 用户点击时会播放相关的环境音

---

## 🎯 常见场景

### 场景1: 编辑一个现有的引导问题

**目标**: 修改引导点的文本为"您还记得那天的天气吗？"

**操作**:
1. 打开编辑器
2. 找到引导点列表
3. 在文本框中修改内容
4. 点击保存

### 场景2: 为北方添加风声环境音

**目标**: 在北方 (NORTH) 添加风声

**操作**:
1. 打开编辑器
2. 找到 NORTH 方向卡片
3. 点击上传按钮选择风声音频
4. 在文本框输入 "微风声"
5. 点击保存

### 场景3: 添加一个新的语音互动点

**目标**: 在空间添加一个可交互的音频点

**操作**:
1. 打开编辑器
2. 点击 "+ 添加语音点"
3. 输入点击时显示的文字
4. 选择关联的环境音 (例如 NORTH 的风声)
5. 点击保存

---

## ⚙️ 高级技巧

### 链接引导点和语音点
- 多个引导点/语音点可以关联同一个环境音
- 这样用户无论点击哪个，都会听到相同的环境音

### 音频配置建议

**背景音乐**:
- 用于烘托整体氛围
- 建议 3-5 分钟长度，设置循环

**环境音** (四个方向):
- 不同方向不同效果，根据空间创意设计
- 建议 2-3 个不同的环境音

**语音点**:
- 关联具体方向的环境音
- 增加用户探索的奖励感

---

## ❌ 常见问题排查

### Q: 编辑后保存，但返回后发现没有保存？
**A**: 
- 确保点击了 **"💾 保存"** 按钮
- 检查浏览器控制台 (F12) 是否有错误提示
- 尝试刷新页面

### Q: 上传的音频文件去了哪里？
**A**: 
- 音频文件被假定存储在 `/assets/audio/` 目录
- 目前版本不支持实际上传到服务器
- 建议后续与后端API集成

### Q: 可以编辑引导点/语音点的位置吗？
**A**: 
- 当前版本**不支持**直接编辑位置
- 位置信息仅供参考
- 如需更改位置，建议在3D场景中直接交互 (未来功能)

### Q: 如何删除整个空间？
**A**: 
- 在选择器中点击空间旁的 **"×"** (删除按钮)
- 这会删除整个空间及其所有数据

---

## 💾 数据保存

### 自动保存位置
- 所有编辑数据自动保存到浏览器 **localStorage**
- 不需要手动同步或上传

### 备份建议
- 定期导出空间配置 (未来功能)
- 清除浏览器缓存前备份数据

---

## 🎨 创意灵感

### 记忆空间设计示例

```
空间名称: "校园回忆"

背景音乐: 青春回忆钢琴曲

NORTH (北): 下课铃声 - "还记得放学的铃声吗？"
SOUTH (南): 操场欢呼 - "那个午后的操场"
EAST (东): 教室讨论 - "课堂讨论的声音"
WEST (西): 宿舍闲聊 - "室友的笑声"

引导点:
- "你最喜欢的角落在哪里？"
- "记得那场校运会吗？"

语音点:
- "点击听听校歌"
- "回放班级合唱"
```

---

## 📚 相关文档

- [项目README](README.md)
- [编辑功能实现](EDITING_FEATURE.md)
- [项目开发说明](README.md)
