# 🧪 空间编辑功能 - 测试清单

## 环境准备

### 前置条件
- Node.js 18+ 已安装
- Maven 3.8+ 已安装
- JDK 17+ 已安装

### 启动项目

```bash
# 终端1 - 启动后端
cd backend
mvn.cmd spring-boot:run  # Windows
# 或 ./mvnw spring-boot:run  # Linux/Mac

# 终端2 - 启动前端
cd frontend
npm install
npm run dev
```

访问 `http://localhost:3000`

---

## 功能测试

### ✅ 测试1: 打开编辑器

**步骤**:
1. 进入应用
2. 点击"进入此刻"
3. 在空间选择器中找到一个空间
4. 点击该空间旁边的 **⚙️ 编辑按钮**

**预期结果**:
- 编辑器面板出现在屏幕中央
- 编辑器显示该空间的所有数据
- 背景变暗 (遮罩层)

**验证**:
- 编辑器标题显示"编辑空间"
- 看到所有编辑区域: BGM、环境音、引导点、语音点

---

### ✅ 测试2: 编辑背景音乐

**步骤**:
1. 打开编辑器
2. 在"🎵 背景音乐"区域点击 **"上传背景音乐"**
3. 选择一个音频文件 (或使用测试音频)
4. 点击 **"清除"** 按钮

**预期结果**:
- 上传后文件名显示在 "当前:" 后面
- 清除后显示 "未设置"

**验证**:
- ✅ 文件名正确显示
- ✅ 清除后恢复为"未设置"

---

### ✅ 测试3: 编辑环境音

**步骤**:
1. 打开编辑器
2. 找到 "🌍 环境音效" 部分
3. 对于每个方向 (NORTH/SOUTH/EAST/WEST):
   - 点击 **"上传"** 按钮
   - 选择音频文件
   - 在文本框输入描述 (如 "雨声", "风声")

**预期结果**:
- 每个方向可以独立上传不同的音频
- 文件名显示在卡片中
- 文本描述正确保存

**验证**:
```
NORTH: 雨声 (ambience_north_xxxx.mp3)
SOUTH: 风声 (ambience_south_xxxx.mp3)
EAST: 鸟鸣 (ambience_east_xxxx.mp3)
WEST: 水流 (ambience_west_xxxx.mp3)
```

---

### ✅ 测试4: 编辑引导点

**步骤**:
1. 打开编辑器
2. 找到 "💬 引导点" 部分
3. 点击 **"+ 添加引导点"**
4. 编辑文本框，输入: "你最喜欢这个地方的哪个角落？"
5. 从下拉菜单选择一个环境音关联
6. 点击 **"保存"**
7. 再次打开编辑器，验证数据已保存

**预期结果**:
- 新引导点被添加到列表
- 文本和关联的环境音正确显示
- 刷新页面后数据仍然存在

**验证**:
- ✅ 添加成功
- ✅ 删除按钮正常工作
- ✅ localStorage 持久化成功

---

### ✅ 测试5: 编辑语音点

**步骤**:
1. 打开编辑器
2. 找到 "🔊 语音点" 部分
3. 点击 **"+ 添加语音点"**
4. 输入文本: "点击听听那时的欢笑声"
5. 关联一个环境音
6. 保存

**预期结果**:
- 语音点被添加
- 文本和关联信息正确保存

**验证**:
- ✅ 添加成功
- ✅ 多个语音点可以共存
- ✅ 数据持久化

---

### ✅ 测试6: 保存和取消

**步骤A - 保存**:
1. 打开编辑器
2. 修改任何字段
3. 点击 **"💾 保存"**
4. 看到提示 "✅ 空间数据已保存！"
5. 编辑器自动关闭

**步骤B - 取消**:
1. 打开编辑器
2. 修改数据
3. 点击 **"✕"** 或 **"取消"**
4. 编辑器关闭，修改被丢弃

**验证**:
- ✅ 保存成功时出现提示
- ✅ 保存后关闭编辑器
- ✅ 取消不会保存修改

---

### ✅ 测试7: 响应式设计

**步骤**:
1. 打开编辑器
2. 改变浏览器窗口大小
3. 测试手机尺寸 (Chrome DevTools)

**预期结果**:
- 编辑器响应式调整
- 在移动设备上仍可正常使用
- 没有水平滚动条（除非必要）

**验证**:
- ✅ 桌面版正常显示
- ✅ 平板版自适应
- ✅ 手机版可用

---

### ✅ 测试8: 数据完整性

**步骤**:
1. 创建一个完整的空间配置:
   - BGM: "background_music.mp3"
   - 4个方向都有环境音
   - 2个引导点
   - 2个语音点
2. 保存
3. 刷新页面
4. 再次打开编辑器

**预期结果**:
- 所有数据完整恢复
- 没有数据丢失或损坏

**验证**:
- ✅ 所有字段值正确
- ✅ 关联关系保持
- ✅ 文件路径完整

---

## 浏览器控制台检查

### 打开开发者工具
按 `F12` 或 `右键 > 检查`

### 检查点

1. **Console (控制台)**:
   - 没有红色错误信息
   - 可能有警告是正常的

2. **Network (网络)**:
   - 编辑保存时应该看不到网络请求 (因为用localStorage)
   - 如果有 `/api/` 请求失败是正常的 (后端可选)

3. **Application > Local Storage**:
   ```
   Key: the-moment-spaces
   Value: {
     "savedSpaces": [...],
     "deletedIds": [...]
   }
   ```

---

## 问题排查

### 编辑器不出现

**可能原因**:
- JavaScript 错误 - 检查控制台
- CSS 冲突 - 检查样式是否加载
- DOM 未就绪 - 等待页面完全加载

**解决**:
```javascript
// 在控制台测试
if (window.initSpaceEditor) {
  console.log('编辑器已加载');
} else {
  console.error('编辑器未加载');
}
```

### 保存失败

**可能原因**:
- localStorage 已满
- 浏览器禁用 localStorage
- 数据格式错误

**解决**:
```javascript
// 清理旧数据
localStorage.removeItem('the-moment-spaces');
// 或查看错误
try {
  localStorage.setItem('test', '{}');
  console.log('localStorage 正常');
} catch(e) {
  console.error('localStorage 错误:', e);
}
```

### 编辑按钮不显示

**可能原因**:
- selector.js 未更新
- CSS 样式不加载
- 回调函数未绑定

**解决**:
```javascript
// 检查空间对象
console.log(spacesData);
// 检查选择器
console.log(document.getElementById('selector'));
```

---

## 性能检查

### 测试多个编辑操作

```
1. 快速打开/关闭编辑器 10次
2. 添加大量引导点 (20+)
3. 保存大型空间
4. 长时间保持编辑器打开
```

**预期**:
- 无明显卡顿
- 内存使用稳定
- 保存响应迅速 (<1秒)

---

## 可选增强测试

### 🎨 UI/UX 检查
- [ ] 颜色方案是否统一
- [ ] 字体是否清晰
- [ ] 按钮是否易点击
- [ ] 错误提示是否清晰

### ♿ 无障碍测试
- [ ] 键盘导航是否正常
- [ ] 屏幕阅读器是否识别
- [ ] 颜色对比是否足够

### 🌐 跨浏览器测试
- [ ] Chrome/Edge (Chromium)
- [ ] Firefox
- [ ] Safari

---

## 提交前检查表

- [ ] 所有功能测试通过
- [ ] 没有控制台错误
- [ ] localStorage 数据正确
- [ ] 代码风格一致
- [ ] 文档完整准确
- [ ] 没有硬编码值 (除配置文件)
- [ ] 处理了边界情况

---

## 快速演示脚本

```bash
# 1. 启动项目
cd backend && mvn spring-boot:run &
cd frontend && npm run dev &

# 2. 打开浏览器
# http://localhost:3000

# 3. 操作流程
# - 点击"进入此刻"
# - 点击一个空间
# - 点击⚙️编辑按钮
# - 修改数据
# - 点击保存
# - 验证更改已保存
```

---

## 完成标记

测试日期: ___________
测试员: ___________

- [ ] 功能测试全部通过
- [ ] 文档完整准确
- [ ] 代码审查通过
- [ ] 准备生产部署

**备注**: _________________________________
