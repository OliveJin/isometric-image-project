# 在空间球内编辑引导点 - 功能设计方案

## 📋 功能需求

### 用户场景

```
用户进入空间球
    ↓
看到全景图和引导点（紫色球体）
    ↓
点击右上角 ✏️ 编辑按钮
    ↓
进入编辑模式（引导点变成粉红色并高亮）
    ↓
在球体内部直接拖动引导点修改位置
    ↓
文本标签显示在点旁边
    ↓
点击"✅ 保存编辑" 或 按 ESC 完成
    ↓
返回正常模式
```

---

## 🎨 UI设计

### 进入空间时的状态

```
┌─────────────────────────────────────┐
│  ← 返回      🎵 BGM    ✏️ 编辑     │  ← 新增编辑按钮
├─────────────────────────────────────┤
│                                     │
│        [全景球体内部]               │
│        ◉ ← 引导点(紫色)             │
│                                     │
│                                     │
└─────────────────────────────────────┘
```

### 进入编辑模式时的状态

```
┌─────────────────────────────────────┐
│  ← 返回    ✏️ 编辑中...  ✅ 完成    │  ← 改变按钮
├─────────────────────────────────────┤
│                                     │
│        [全景球体内部]               │
│        ◉ ← 粉红色亮点              │
│    [点的文本]                       │
│                                     │
│   💡 提示：拖动点修改位置           │
│      按 ESC 退出编辑                │
└─────────────────────────────────────┘
```

---

## 🛠️ 技术实现方案

### 新增UI控件

#### 1. 编辑按钮 (在球体内显示)

```html
<div id="spaceEditButton" class="space-edit-button">
  <button id="toggleEditModeBtn" class="btn-edit-mode">✏️ 编辑</button>
  <button id="saveEditModeBtn" class="btn-edit-mode-save hidden">✅ 完成</button>
</div>
```

#### 2. 编辑提示 (编辑模式时显示)

```html
<div id="editModeTooltip" class="edit-mode-tooltip hidden">
  💡 拖动点修改位置 | 按 ESC 完成编辑
</div>
```

---

## 📁 代码结构

### 需要修改的文件

```
frontend/src/
├── main.js
│   ├── 添加编辑模式状态
│   ├── 添加编辑按钮事件
│   └── 集成编辑功能
│
├── scene/
│   ├── guidePoints.js (已有)
│   │   └── 支持编辑模式高亮
│   │
│   └── pointEditor3DInSpace.js (新增)
│       ├── 进入编辑模式
│       ├── 拖动修改位置
│       ├── 显示标签
│       └── 保存编辑
│
├── ui/
│   └── SpaceEditUI.js (新增)
│       ├── 显示/隐藏编辑按钮
│       ├── 显示编辑提示
│       └── 处理编辑事件
│
└── style/
    └── space-edit.css (新增)
        ├── 编辑按钮样式
        └── 编辑提示样式
```

---

## 🔄 工作流程

### 状态转换

```
NORMAL 状态
├── 显示编辑按钮
├── 显示引导点(紫色)
└── 用户点击 ✏️

    ↓ 转换到 EDITING

EDITING 状态
├── 隐藏编辑按钮
├── 显示完成按钮
├── 引导点变粉红色
├── 显示标签
├── 用户可拖动点
└── 用户点击 ✅ 或 ESC

    ↓ 转换回 NORMAL
```

---

## 📝 数据流

### 编辑流程的数据变化

```
currentSpace.audio.guidePoints
    ↓
进入编辑模式
    ↓
用户拖动点 → position [x,y,z] 更新
    ↓
实时显示新位置
    ↓
点击完成 → updateSpace(currentSpace)
    ↓
保存到 localStorage
```

---

## 🎯 功能清单

### 需要实现的功能

- [ ] 编辑按钮 (✏️ 编辑、✅ 完成)
- [ ] 进入编辑模式的UI转换
- [ ] 在球体内拖动引导点
- [ ] 实时显示文本标签
- [ ] 坐标显示和更新
- [ ] 编辑提示信息
- [ ] ESC键完成编辑
- [ ] 完成后保存数据

---

## 🎨 视觉设计

### 编辑按钮区域

```
┌────────────────────────┐
│ ← 返回  |  ✏️ 编辑    │
└────────────────────────┘
```

### 编辑模式样式

引导点变化：
- 颜色：#FF6B9D (粉红色)
- 大小：0.3m (更大便于选择)
- 发光强度：1.2
- 光环：明显可见

文本标签：
- 背景：粉红色半透明
- 位置：点的上方
- 内容：点的名称 + 坐标

---

## ✨ 用户体验增强

### 反馈机制

1. **进入编辑模式**
   - 引导点立即变色
   - 显示提示信息
   - 光环开始脉冲

2. **拖动中**
   - 标签跟随鼠标
   - 实时显示坐标
   - 光环随动作闪烁

3. **完成编辑**
   - 显示"已保存"提示
   - 引导点恢复紫色
   - 自动隐藏提示

---

## 🔧 实现步骤

### Step 1: 创建SpaceEditUI模块
- 编辑按钮UI
- 编辑状态管理
- 事件处理

### Step 2: 创建PointEditor3DInSpace模块
- 基于现有pointEditor.js优化
- 进入/退出编辑模式
- 拖动逻辑
- 保存逻辑

### Step 3: 集成到main.js
- 添加编辑状态
- 显示/隐藏编辑按钮
- 处理编辑切换

### Step 4: 添加样式
- 编辑按钮
- 编辑提示
- 过渡动画

---

## 🚀 优势

✅ 更直观的编辑体验  
✅ 用户在实际环境中编辑  
✅ 即时视觉反馈  
✅ 不需要打开编辑菜单  
✅ 减少编辑步骤  

---

这是您想要的设计方向吗？需要我开始实现这个功能吗？
